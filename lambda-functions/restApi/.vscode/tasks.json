{
    "version": "2.0.0",
    "tasks": [
        {
          "label": "restApi - Wait Remote Debugger Server",
          "type": "shell",
          "command": "while [[ -z $(docker ps | grep :5061) ]]; do sleep 1; done; sleep 1; "
        }
    ]
}